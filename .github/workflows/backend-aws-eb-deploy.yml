name: Backend AWS Elastic Beanstalk Deploy Manual

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    name: Build and deploy backend to Elastic Beanstalk
    runs-on: ubuntu-latest
    environment: staging

    env:
      AWS_REGION: ${{ vars.AWS_REGION }}
      AWS_ROLE_TO_ASSUME: ${{ vars.AWS_ROLE_TO_ASSUME }}
      AWS_EB_APPLICATION: ${{ vars.AWS_EB_APPLICATION }}
      AWS_EB_ENVIRONMENT: ${{ vars.AWS_EB_ENVIRONMENT }}
      AWS_EB_DEPLOY_BUCKET: ${{ vars.AWS_EB_DEPLOY_BUCKET }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: "backend/aulafy-api-nest/.nvmrc"
          cache: npm
          cache-dependency-path: backend/aulafy-api-nest/package-lock.json

      - name: Install dependencies
        working-directory: backend/aulafy-api-nest
        # npm install usado temporalmente: package-lock.json tiene entradas nativas
        # (@emnapi/core, @emnapi/runtime) que npm ci rechaza en Linux por desincronizacion
        # de plataforma. Mejora futura: regenerar package-lock en Linux y volver a npm ci.
        run: npm install --no-audit --no-fund

      - name: Run backend tests
        working-directory: backend/aulafy-api-nest
        run: npm test

      - name: Build backend
        working-directory: backend/aulafy-api-nest
        run: npm run build

      - name: Validate deployment variables
        run: |
          test -n "${{ vars.AWS_REGION }}"
          test -n "${{ vars.AWS_ROLE_TO_ASSUME }}"
          test -n "${{ vars.AWS_EB_APPLICATION }}"
          test -n "${{ vars.AWS_EB_ENVIRONMENT }}"
          test -n "${{ vars.AWS_EB_DEPLOY_BUCKET }}"
          echo "Deployment variables are present."

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ vars.AWS_REGION }}
          role-to-assume: ${{ vars.AWS_ROLE_TO_ASSUME }}

      - name: Create Procfile
        working-directory: backend/aulafy-api-nest
        run: |
          echo "web: node dist/main.js" > Procfile

      - name: Prepare version label
        run: |
          SHORT_SHA="${GITHUB_SHA::7}"
          VERSION_LABEL="backend-${SHORT_SHA}-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          ZIP_NAME="${VERSION_LABEL}.zip"
          echo "VERSION_LABEL=${VERSION_LABEL}" >> "$GITHUB_ENV"
          echo "ZIP_NAME=${ZIP_NAME}" >> "$GITHUB_ENV"
          echo "Using version label: ${VERSION_LABEL}"

      - name: Package application for Elastic Beanstalk
        working-directory: backend/aulafy-api-nest
        run: |
          zip -r "${ZIP_NAME}" \
            dist/ \
            package.json \
            package-lock.json \
            Procfile

      - name: Upload artifact to S3
        working-directory: backend/aulafy-api-nest
        run: |
          aws s3 cp \
            "${ZIP_NAME}" \
            "s3://${AWS_EB_DEPLOY_BUCKET}/aulafy-api/${ZIP_NAME}"

      - name: Create Elastic Beanstalk application version
        run: |
          aws elasticbeanstalk create-application-version \
            --application-name "${AWS_EB_APPLICATION}" \
            --version-label "${VERSION_LABEL}" \
            --source-bundle "S3Bucket=${AWS_EB_DEPLOY_BUCKET},S3Key=aulafy-api/${ZIP_NAME}" \
            --no-auto-create-application

      - name: Deploy to Elastic Beanstalk environment
        run: |
          aws elasticbeanstalk update-environment \
            --application-name "${AWS_EB_APPLICATION}" \
            --environment-name "${AWS_EB_ENVIRONMENT}" \
            --version-label "${VERSION_LABEL}"

      - name: Wait for environment to become healthy
        run: |
          aws elasticbeanstalk wait environment-updated \
            --application-name "${AWS_EB_APPLICATION}" \
            --environment-names "${AWS_EB_ENVIRONMENT}"
