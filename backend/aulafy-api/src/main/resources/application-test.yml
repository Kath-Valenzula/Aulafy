spring:
  datasource:
    url: jdbc:h2:mem:aulafy_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: false

app:
  security:
    jwt:
      secret: aulafy-test-secret-key-change-in-real-environments
